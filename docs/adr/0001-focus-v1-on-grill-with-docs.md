# Focus v1 on grill-with-docs

Barbequeue v1 extends the `grill-with-docs` workflow with participant discussion and session consensus instead of acting as a generic multiplayer wrapper for any Codex skill. This keeps the product anchored to a concrete repo-grounded planning workflow while leaving room to generalize later if repeated use shows the same collaboration model applies elsewhere.
