# Barbequeue owns session invocation

Hosts start collaborative grilling by invoking Barbequeue as the primary experience, while Barbequeue applies the `grill-with-docs` workflow inside the session. This avoids requiring hosts to understand Codex plugin and skill composition, and it gives Barbequeue a stable product surface even though its first version is intentionally specific to `grill-with-docs`.
