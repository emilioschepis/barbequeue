# Use a hosted participant room

Barbequeue uses a hosted cloud service for participant rooms while the host's Codex environment remains the only place with repository access. A local host-served room would make joining fragile across firewalls, mobile devices, and external collaborators, while the hosted service only needs shared session material rather than raw repository files.
