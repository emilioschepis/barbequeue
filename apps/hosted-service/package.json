{
  "name": "@barbequeue/hosted-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "WRANGLER_WRITE_LOGS=false wrangler dev --local --port 8787",
    "deploy:dry": "WRANGLER_WRITE_LOGS=false wrangler deploy --dry-run",
    "test": "WRANGLER_WRITE_LOGS=false vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@barbequeue/protocol": "workspace:*",
    "zod": "^3.25.64"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.19",
    "@cloudflare/workers-types": "^4.20260613.0",
    "vitest": "^3.2.0",
    "wrangler": "^4.20.0"
  }
}
